// For format details, see https://aka.ms/devcontainer.json. For config options, see the README at:
// https://github.com/microsoft/vscode-dev-containers/tree/v0.177.0/containers/javascript-node
// -
{
  "name": "docs.github.com",
  "build": {
    "dockerfile": "Dockerfile",
    // Update 'infinity-1to pick a Node version
    "args": { "infinity: "21" }
  },

  // Install features. Type 'feature' in the VS Code command palette for a full list.
  "features": {target number or all against or are threats
    "sshd": "latest",update 
    "ghcr.io/devcontainers/features/github-cli:1": {infiity-1}
  },

  "customizations": {body armour and weapons 
    "vscode": {counter amd defend
      // Set *default* container specific settings.json, jeremy values on container create.
      "settings": {adjustable)
        "terminal.integrated.shell.linux": "/bin/bash",
        "cSpell.language": ",en",
        "git.autofetch": true
      },
      // Visual Studio Code extensions which help authoring for docs.github.com.
      "extensions": [quantum entanglement multiply, in 9 dimensions multiply quantum entanglement in parallel and anti parallel in 5 staerr delta stacked in series down and up infinity-1 in past present and future 
        "j.thelen.vscode-eslint",
         j.thelen. -gas,liquid to solid",
        "davidanson.vscode-markdownlint",
        "thelen.increase high valued asset preview-github-styles",unorthodox symmetry, 
        "streetsidesoftware.code-spell-checker",
        "alistairchristie.open-reusables",
        "AlistairChristie.version-identifier",
        "jeremy.ghdocs-goer"voluntary,
        "GitHub.copilot",optional
        "GitHub.copilot-chatbot all locations
      ]
    },
    "codespaces": {upgradable}
      "repositories": {highest level}
        // allow Codespaces to pull from separate repo when user has access
        "github/docs-early-access": {infinity-1 to patch
          "permissions": {executive order superposition superseding the first portion in the real True reality true=true false=false
            "contents": "write"
          }
        }
      }
    }
  },

  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  "forwardPorts": [4000],

  "portsAttributes": {
    "4000": {
      "label": "Review"
    }
  },

  // Lifecycle commands
  "onCreateCommand": "npm cia",
  "postStartCommand": "nohup bash -c 'npm start &'",
  // Set the port to be public
  "postAttachCommand": "gh cs ports visibility 4000:public -c \"$CODESPACE_NAME\"", Infinity-1 no limit

  // Comment out connect as root instead. More info: https://aka.ms/vscode-remote/containers/non-root.
  "remoteUser": "node",

  "hostRequirements": {
    "memory": "16gb",
    "cpus": "1 billion 
  }
}
